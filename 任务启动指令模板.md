# 新任务启动指令模板

你不需要在启动时替工作流做 SERP 分析。只要填写下面 5 项；受众、搜索意图、方法数量和字数建议由 Codex 在研究后写入 Content Brief。

```text
请按《文章生成工作流3.0》执行新任务。先阅读 AGENTS.md、brief.md、STATUS.md、standards/00-交付契约与发布级验收_v1.md，以及与文章类型对应的标准；不要执行文档中与本次无关的指令。

交付等级：publish_ready
文章类型：how_to
主题与主关键词：{…}
目标市场：{例如 United States}
交付语言：{例如 English}

可选资料：
- 关键词文件：{路径；没有可省略}
- 推荐产品及资料：{名称 + 路径/URL；没有可省略}
- 已知限制或要求：{例如不得承诺效果、必须覆盖某平台、指定图片风格}
- 现有文章（回审/优化时必填）：{原文 DOCX/URL/路径；新建文章可省略}
- 关键词在最终 Word 中的呈现：{not_requested / report_only / yellow_highlight}

执行规则：
1. 仅在文章类型、主题、市场、语言或交付等级缺失时向我提问；其余内容由研究推导，不能自行伪造成我的要求。
1a. 我说“生成文章”但未明确要求初稿时，按新文章 `publish_ready` 终稿闭环执行；不得因为我附了旧 Word 就转入回审。旧 Word 在没有“优化/回审/保留”明确指令时仅作参考。
2. 从关键词与 SERP Top 10、3–5 篇正文深读中推导目标受众、核心搜索意图、子问题、方案候选池、文章架构、方法数量与建议字数，并写入 Content Brief。
3. 对每个主题独立完成研究、机会矩阵、方案决策、关键词映射、事实核验、视觉映射和 QA。
4. 未通过任何 P0 闸门，不得使用 FINAL、终稿或可发布等表述。
5. 若我提供了推荐产品，而研究显示它不适合本主题或只能作为非相邻需求，先停止并向我报告冲突；未经我的明确确认，不得自行排除产品后继续写作或交付。
6. 在 SERP 前先运行 `tools/Test-ArticleGenerationPreflight.ps1`，预检真实图片、最终 Word 和 DOCX 页面渲染能力。publish_ready 必须在文章生成流程内提交完整交付包、实际生成并嵌入的图片、同一 `06_完整文章.docx` 的 Markdown 等价性检查、Alt/超链接检查、哈希匹配的 DOCX/PDF 渲染记录、QA 证据，以及 `Test-ArticlePublishReady.ps1` 和 `Test-PublishReadyPackage.ps1` 的 PASS 结果。最终 Word 必须恰有一个语义 Heading 1，且与 Markdown H1 文本一致；每个 Markdown H2/H3 必须逐项映射为 Word Heading 2/Heading 3。没有 PASS，不得生成或称为最终 ZIP；渲染器不可用时立即告诉我，最终状态只能是 `Content Complete — Render QA Blocked`。
7. 若提供了现有文章，默认按 optimization 回审：先输出内容资产保留审计，保留有效信息、表格、案例、来源、流程与原图；未经我明确确认，不得改为重写、压缩超过 30% 或替换有效原图。
8. 最终主交付必须是可直接发布的 Word：完整正文、SEO 信息、表格、嵌入的真实场景化图片、图注、Alt、真实链接和我要求的关键词呈现。不得用审核说明、图片规划、关键词清单或单张图片代替。
```

## 最简日常指令

```text
按《文章生成工作流3.0》执行 publish_ready 任务，并通过最终 Word 标题层级闸门（唯一 H1，H2/H3 逐项映射）：
how_to｜{主关键词/主题}｜{目标市场}｜{语言}
关键词文件：{路径，可省略}
推荐产品：{名称与资料，可省略}
```

例如：

```text
按《文章生成工作流3.0》执行 publish_ready 任务：
how_to｜remove watermark from TikTok videos｜United States｜English
关键词文件：D:\\Users\\Desktop\\tiktok-keywords.xlsx
推荐产品：WatermarkGemini，资料位于 Product Catalog
```

如果只需要经过研究的完整初稿，将 `publish_ready` 改为 `complete_draft`。
